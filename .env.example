# Environment Configuration Example
# Copy this file to .env and update values

# Environment: development | production
ENV=development

# Secret key for JWT (generate a secure key for production!)
# You can generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-super-secret-key-here

# Docker PostgreSQL Configuration
POSTGRES_USER=one2talk
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=one2talk_db

# Database Configuration
# For SQLite (local development): sqlite:///./votes.db
# For PostgreSQL (production): postgresql://user:password@host:port/database
# For Docker: postgresql://one2talk:password@db:5432/one2talk_db (auto-configured by docker-compose)
DATABASE_URL=sqlite:///./votes.db

# Server Configuration  
HOST=0.0.0.0
PORT=4040

# Default Admin Credentials (for Docker auto-creation)
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=12345678
